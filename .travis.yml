language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-restfully' ]; then cd .. && eval "mv $currentfolder generator-restfully" && cd generator-restfully; fi

